default:
  servers:
    - host: "1.1.1.1"
      user: pp_tristanf
      pass: "mYpAssW0rD"
      groups:
        - prod
        - backoffice
    - host: "2.2.2.2"
      user: michelangelo
      pass: "nope"
      groups:
        - prod
        - front
    - host: "sql1"
      port: 8889
      user: root
      password: root
      groups:
        - local
    - host: "sql2"
      user: root
      password: ''
      groups:
        - local
  query:
    # Timeout per query, in miliseconds.
    timeout: false
  output:
    diffonly: false
  table:
    wordwrap: 60